MOV:
    -kopiert Daten von einer Quelle zum Ziel
    -mov Ziel, Quelle
ADD:
    -addiert dem Ziel die Sachen aus der Quelle
    -add Ziel, Quelle
SUB:
    -subtrahiert von dem Ziel die Sachen aus der Quelle
    -sub Ziel, Quelle
JMP:
    -springt IMMER an die angegebene Stelle
    im Code
    -jmp Ziel
JE:
    -jump if equal
    -springt nur, wenn das Zero Flag(ZF) = 1 ist
    mov AL, 5
    cmp AL, 5     ; vergleiche AL mit 5
    je  GLEICH    ; springt zu GLEICH, weil AL == 5
CMP:
    -vergleicht Quelle und Ziel, indem es sie
    beide intern subtrahiert
    -Flags im Prozessor werden gesetzt
    (z.B. Zero Flag, Sign Flag, Carry Flag, Overflow Flag)
    mov AL, 5
    cmp AL, 5      ; vergleicht AL mit 5 (Zero Flag wird gesetzt)
    je  GLEICH     ; springt, weil ZF=1 (5 == 5)
CALL:
    -ruft eine Funktion auf
    -call sub_one   ;ruft die Funktion auf
    ...
    sub_one 
    dec RAX
    ret             ;kehrt zurück
JNE:
    -jump if not equal
    -Gegenteil zu je
LEA:
    -lea rax, [n]  ; rax = Adresse von n
    -Nach dieser Zeile enthält rax nicht den Wert 10,
     sondern die Adresse von n
    -bei mov al, [rax] liest du dann den Wert 10
HLT:
    -stoppt die Ausführung des Prozessors bis ein Interrupt eintrifft
    -CPU geht in den Stromsparmodus
    -hält die CPU an wenn sie nichts zu tun hat
PUSH:
    -push legt einen Wert auf den Stack
    -updatet alle Pointer
    -CPU benutzt ihn, um:
        Rücksprungadressen zu speichern (call)
        lokale Variablen temporär zu speichern
        Register zu sichern
POP:
    -nimmt den Wert vom Stack
CLI:
    -Clear Interrupt Flag
    -deaktiviert Interrupts
STI:
    -Set Interrupt Flag
    -erlaubt wieder Interrupts
